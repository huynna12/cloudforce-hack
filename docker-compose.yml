services:
  backend:
    build: ./backend
    env_file: .env
    ports:
      - "8080:8080"
    restart: unless-stopped

  frontend:
    build: ./frontend
    env_file: .env
    ports:
      - "3000:3000"
    restart: unless-stopped
    depends_on:
      - backend
